CREATE DATABASE IF NOT EXISTS ingresse_ai;
USE ingresse_ai;

CREATE TABLE usuarios (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    nome_completo VARCHAR(255) NOT NULL,
    cpf_cnpj VARCHAR(20) NOT NULL UNIQUE,
    senha VARCHAR(255) NOT NULL,
    tipo ENUM('COMPRADOR', 'VENDEDOR') NOT NULL,
    criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE eventos (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    vendedor_id BIGINT NOT NULL,
    titulo VARCHAR(255) NOT NULL,
    descricao TEXT NOT NULL,
    tipo_evento VARCHAR(50) NOT NULL, 
    data_evento DATETIME NOT NULL,
    criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (vendedor_id) REFERENCES usuarios(id)
);

CREATE TABLE ingressos (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    evento_id BIGINT NOT NULL,
    quantidade INT NOT NULL CHECK (quantidade <= 4),
    valor DECIMAL(10, 2) NOT NULL,
    FOREIGN KEY (evento_id) REFERENCES eventos(id)
);

CREATE TABLE compras (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    comprador_id BIGINT NOT NULL,
    ingresso_id BIGINT NOT NULL,
    quantidade_comprada INT NOT NULL CHECK (quantidade_comprada <= 4),
    data_compra TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (comprador_id) REFERENCES usuarios(id),
    FOREIGN KEY (ingresso_id) REFERENCES ingressos(id)
);

INSERT INTO usuarios (nome_completo, cpf_cnpj, senha, tipo) 
VALUES ('Vendedor Oficial', 'A1.B2C.D3E/0001-F4', '$2a$10$W2n...HashBCryptExemplo', 'VENDEDOR');

SET @vendedor_id = LAST_INSERT_ID();

INSERT INTO eventos (vendedor_id, titulo, descricao, tipo_evento, data_evento) VALUES
(@vendedor_id, 'Festival de Tecnologia', 'Evento sobre desenvolvimento e segurança', 'Conferência', '2026-11-15 09:00:00'),
(@vendedor_id, 'Show Acústico', 'Apresentação ao vivo', 'Show', '2026-12-10 20:00:00');

INSERT INTO ingressos (evento_id, quantidade, valor) VALUES
(1, 4, 150.00),
(2, 4, 80.00);